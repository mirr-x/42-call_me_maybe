[project]
name = "call-me-maybe"
version = "0.1.0"
description = "Function-calling project using constrained decoding"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "numpy",
    "pydantic",
    "llm-sdk",
    "accelerate",
    "wrapt",
]

[tool.uv.sources]
llm-sdk = { path = "./llm_sdk" }
